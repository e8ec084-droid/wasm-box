# Vendored WASI runtime (26 MB) — fetched via scripts/fetch_runtime.sh
runtime/*.wasm
runtime/*.sha256sum

# Compiled plugin output (generated, not source)
plugins/*/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/

# Captured execution output
*.stdout
*.stderr
